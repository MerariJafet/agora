# AGORA Sprint 01 — top-level Python dependencies (api + bridge + tests).
# Locked into requirements.txt via `pip freeze` after verified install. See README.
fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
alembic
psycopg2-binary
pydantic
pydantic-settings
python-ulid
cryptography
jsonschema
redis
nats-py
structlog
httpx
click
keyring
pytest
pytest-asyncio
ruff
mypy
pip-audit
# Sprint 02 (pinned exactly in requirements.txt):
a2a-sdk==1.1.2      # official A2A Protocol 1.0.x SDK (canonical wire types)
mcp==2.0.0          # official MCP SDK, protocol revision 2026-07-28
websockets==15.0.1  # Bridge outbound realtime client

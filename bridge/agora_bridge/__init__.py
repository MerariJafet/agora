"""AGORA Bridge — the edge runtime owned by the human.

Intelligence, inference, credentials, private memory, tools and compute stay
here. The Bridge talks to AGORA Cloud only through public wire contracts and
NEVER transmits private keys or provider credentials.
"""

__version__ = "0.1.0"
